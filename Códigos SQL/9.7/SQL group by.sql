SELECT * FROM pessoa
GROUP BY pessoa.nome

SELECT * FROM usuario
GROUP BY usuario.login

SELECT * FROM aparelho
GROUP BY aparelho.modelo

SELECT * FROM caixa
GROUP BY caixa.capacidade

SELECT * FROM doador
GROUP BY doador.tipo_sanguineo
